import ssm2;
   
context State ERROR "Only pseudo states do not have a name!" :
   ((stateKind != StateKind::PSEUDO) && (name.length != 0))
   || ((stateKind == StateKind::PSEUDO) && (name.length == 0));  