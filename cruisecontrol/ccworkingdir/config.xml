<cruisecontrol>
  <!-- ############################################################################################################# -->
  <!-- ###########                NEW PROJECT: helloworld                                 ########################## -->
  <!-- ############################################################################################################# -->
  <project name="helloworld" buildafterfailed="true">
    <listeners>
      <currentbuildstatuslistener file="logs/${project.name}/status.txt"/>
    </listeners>

    <!-- Bootstrappers are run every time the build runs, *before* the modification checks -->
    <bootstrappers>
	<!-- Handles updating a single file (typically build.xml and/or build.properties) from a Subversion repository before the build begins. -->
    </bootstrappers>

    <!-- Defines where cruise looks for changes, to decide
         whether to run the build (specified in seconds) -->
    <modificationset quietperiod="1800">
      <svn localworkingcopy="checkout/${project.name}"/>
 	<!-- https://rtsys.informatik.uni-kiel.de/svn/teaching/prak/07ws-model/public/quality-assurance/helloworld -->
    </modificationset>

    <!-- Configures the actual build loop, how often and which
         build file/target (seconds to sleep in between polling for changes) -->
    <schedule interval="3600">
      <ant anthome="/usr/share/ant"
           buildfile="build-${project.name}.xml"
           target="metrics"
           uselogger="false"
           usedebug="false"/>
    </schedule>

    <!-- directory to write build logs to -->
    <log logdir="logs/${project.name}"/>

    <!-- Publishers are run *after* a build completes -->
    <publishers>

	<!--
	<email mailhost="mail.informatik.uni-kiel.de"
		returnaddress="tiet@informatik.uni-kiel.de"
		buildresultsurl="http://rtsys.informatik.uni-kiel.de/cc/buildresults/${project.name}"
		skipusers="true" spamwhilebroken="true">
	    <always address="tiet@informatik.uni-kiel.de"/>
	    <failure address="tiet@informatik.uni-kiel.de"/>
	</email>
	-->

<!--
If you also want individual committers to receive email for all builds where they made changes, then set skipusers="false", and add a <map alias="cvsuser" address="cvsuser@mydomain.com"/>  element for each user inside the <email> element (replacing cvsuser with the real user id and email).
-->
	<htmlemail mailhost="mail.informatik.uni-kiel.de"
		returnaddress="tiet@informatik.uni-kiel.de"
		defaultsuffix="@informatik.uni-kiel.de"
		buildresultsurl="http://rtsys.informatik.uni-kiel.de:8180/cruisecontrol/buildresults/${project.name}"
		skipusers="false"
		spamwhilebroken="false"
		css="/home/local/cruisecontrol/cruisecontrol-2-7-1/reporting/jsp/webcontent/css/cruisecontrol.css"
		xsldir="/home/local/cruisecontrol/cruisecontrol-2-7-1/reporting/jsp/webcontent/xsl"
		logdir="logs/${project.name}">

		<map alias="tiet" address="tiet@informatik.uni-kiel.de" />
		<!--<map alias="oba" address="oba@informatik.uni-kiel.de" />-->
		<!--<map alias="haf" address="haf@informatik.uni-kiel.de" />-->
		
		<always address="tiet" />
		<!--<failure address="oba" reportWhenFixed="true" />-->
		<!--<failure address="haf" reportWhenFixed="true" />-->

	</htmlemail>

	<artifactspublisher
	    dir="checkout/${project.name}/target"
	    dest="artifacts/${project.name}"/>

    </publishers>
  </project>



  <!-- ############################################################################################################# -->
  <!-- ###########                NEW PROJECT: edu.unikiel.rtsys.gmf.caaa                 ########################## -->
  <!-- ############################################################################################################# -->
  <project name="edu.unikiel.rtsys.gmf.caaa" buildafterfailed="true">
    <listeners>
      <currentbuildstatuslistener file="logs/${project.name}/status.txt"/>
    </listeners>

    <!-- Bootstrappers are run every time the build runs, *before* the modification checks -->
    <bootstrappers>
        <!-- Handles updating a single file (typically build.xml and/or build.properties) from a Subversion repository before the build begins. -->
    </bootstrappers>

    <!-- Defines where cruise looks for changes, to decide whether to run the build (specified in seconds) -->
    <modificationset quietperiod="1800">
      <svn localworkingcopy="checkout/${project.name}"/>
        <!-- https://rtsys.informatik.uni-kiel.de/svn/teaching/prak/07ws-model/public/phase2/group3-cwu-jhed/stable/edu.unikiel.rtsys.gmf.caaa -->
    </modificationset>

    <!-- Configures the actual build loop, how often and which build file/target (seconds to sleep in between polling for changes) -->
    <schedule interval="3600">
      <ant anthome="/usr/share/ant"
           buildfile="build-${project.name}.xml"
           target="metrics"
           uselogger="false"
           usedebug="false"/>
    </schedule>

    <!-- directory to write build logs to -->
    <log logdir="logs/${project.name}"/>

    <!-- Publishers are run *after* a build completes -->
    <publishers>

        <!--
        <email mailhost="mail.informatik.uni-kiel.de"
                returnaddress="tiet@informatik.uni-kiel.de"
                buildresultsurl="http://rtsys.informatik.uni-kiel.de/cc/buildresults/${project.name}"
                skipusers="true" spamwhilebroken="true">
            <always address="tiet@informatik.uni-kiel.de"/>
            <failure address="tiet@informatik.uni-kiel.de"/>
        </email>
        -->

<!--
If you also want individual committers to receive email for all builds where they made changes, then set skipusers="false", and add a <map alias="cvsuser" address="cvsuser@mydomain.com"/>  element for each user inside the <email> element (replacing cvsuser with the real user id and email).
-->
        <htmlemail mailhost="mail.informatik.uni-kiel.de"
                returnaddress="tiet@informatik.uni-kiel.de"
                defaultsuffix="@informatik.uni-kiel.de"
                buildresultsurl="http://rtsys.informatik.uni-kiel.de:8180/cruisecontrol/buildresults/${project.name}"
                skipusers="false"
                spamwhilebroken="false"
                css="/home/local/cruisecontrol/cruisecontrol-2-7-1/reporting/jsp/webcontent/css/cruisecontrol.css"
                xsldir="/home/local/cruisecontrol/cruisecontrol-2-7-1/reporting/jsp/webcontent/xsl"
                logdir="logs/${project.name}">

                <map alias="tiet" address="tiet@informatik.uni-kiel.de" />
                <!--<map alias="oba" address="oba@informatik.uni-kiel.de" />-->
                <!--<map alias="haf" address="haf@informatik.uni-kiel.de" />-->

                <always address="tiet" />
                <!--<failure address="oba" reportWhenFixed="true" />-->
                <!--<failure address="haf" reportWhenFixed="true" />-->

        </htmlemail>

        <artifactspublisher
            dir="checkout/${project.name}/target"
            dest="artifacts/${project.name}"/>

    </publishers>
  </project>


  <!-- ############################################################################################################# -->
  <!-- ###########                NEW PROJECT: edu.unikiel.rtsys.gmf.templates            ########################## -->
  <!-- ############################################################################################################# -->
  <project name="edu.unikiel.rtsys.gmf.templates" buildafterfailed="true">
    <listeners>
      <currentbuildstatuslistener file="logs/${project.name}/status.txt"/>
    </listeners>

    <!-- Bootstrappers are run every time the build runs, *before* the modification checks -->
    <bootstrappers>
        <!-- Handles updating a single file (typically build.xml and/or build.properties) from a Subversion repository before the build begins. -->
    </bootstrappers>

    <!-- Defines where cruise looks for changes, to decide whether to run the build (specified in seconds) -->
    <modificationset quietperiod="1800">
      <svn localworkingcopy="checkout/${project.name}"/>
        <!-- https://rtsys.informatik.uni-kiel.de/svn/teaching/prak/07ws-model/public/phase2/group5-cho-nma/stable/edu.unikiel.rtsys.gmf.templates -->
    </modificationset>

    <!-- Configures the actual build loop, how often and which build file/target (seconds to sleep in between polling for changes) -->
    <schedule interval="3600">
      <ant anthome="/usr/share/ant"
           buildfile="build-${project.name}.xml"
           target="metrics"
           uselogger="false"
           usedebug="false"/>
    </schedule>

    <!-- directory to write build logs to -->
    <log logdir="logs/${project.name}"/>

    <!-- Publishers are run *after* a build completes -->
    <publishers>

        <!--
        <email mailhost="mail.informatik.uni-kiel.de"
                returnaddress="tiet@informatik.uni-kiel.de"
                buildresultsurl="http://rtsys.informatik.uni-kiel.de/cc/buildresults/${project.name}"
                skipusers="true" spamwhilebroken="true">
            <always address="tiet@informatik.uni-kiel.de"/>
            <failure address="tiet@informatik.uni-kiel.de"/>
        </email>
        -->

<!--
If you also want individual committers to receive email for all builds where they made changes, then set skipusers="false", and add a <map alias="cvsuser" address="cvsuser@mydomain.com"/>  element for each user inside the <email> element (replacing cvsuser with the real user id and email).
-->
        <htmlemail mailhost="mail.informatik.uni-kiel.de"
                returnaddress="tiet@informatik.uni-kiel.de"
                defaultsuffix="@informatik.uni-kiel.de"
                buildresultsurl="http://rtsys.informatik.uni-kiel.de:8180/cruisecontrol/buildresults/${project.name}"
                skipusers="false"
                spamwhilebroken="false"
                css="/home/local/cruisecontrol/cruisecontrol-2-7-1/reporting/jsp/webcontent/css/cruisecontrol.css"
                xsldir="/home/local/cruisecontrol/cruisecontrol-2-7-1/reporting/jsp/webcontent/xsl"
                logdir="logs/${project.name}">

                <map alias="tiet" address="tiet@informatik.uni-kiel.de" />
                <!--<map alias="oba" address="oba@informatik.uni-kiel.de" />-->
                <!--<map alias="haf" address="haf@informatik.uni-kiel.de" />-->

                <always address="tiet" />
                <!--<failure address="oba" reportWhenFixed="true" />-->
                <!--<failure address="haf" reportWhenFixed="true" />-->
        </htmlemail>

        <artifactspublisher
            dir="checkout/${project.name}/target"
            dest="artifacts/${project.name}"/>

    </publishers>
  </project>



  <!-- ############################################################################################################# -->
  <!-- ###########                NEW PROJECT: edu.unikiel.rtsys.gmf.unigaps              ########################## -->
  <!-- ############################################################################################################# -->
  <project name="edu.unikiel.rtsys.gmf.unigaps" buildafterfailed="true">
    <listeners>
      <currentbuildstatuslistener file="logs/${project.name}/status.txt"/>
    </listeners>

    <!-- Bootstrappers are run every time the build runs, *before* the modification checks -->
    <bootstrappers>
        <!-- Handles updating a single file (typically build.xml and/or build.properties) from a Subversion repository before the build begins. -->
    </bootstrappers>

    <!-- Defines where cruise looks for changes, to decide whether to run the build (specified in seconds) -->
    <modificationset quietperiod="1800">
      <svn localworkingcopy="checkout/${project.name}"/>
        <!-- https://rtsys.informatik.uni-kiel.de/svn/teaching/prak/07ws-model/public/phase2/group2-mim-schm/stable/edu.unikiel.rtsys.gmf.unigaps -->
    </modificationset>

    <!-- Configures the actual build loop, how often and which build file/target (seconds to sleep in between polling for changes) -->
    <schedule interval="3600">
      <ant anthome="/usr/share/ant"
           buildfile="build-${project.name}.xml"
           target="metrics"
           uselogger="false"
           usedebug="false"/>
    </schedule>

    <!-- directory to write build logs to -->
    <log logdir="logs/${project.name}"/>

    <!-- Publishers are run *after* a build completes -->
    <publishers>

        <!--
        <email mailhost="mail.informatik.uni-kiel.de"
                returnaddress="tiet@informatik.uni-kiel.de"
                buildresultsurl="http://rtsys.informatik.uni-kiel.de/cc/buildresults/${project.name}"
                skipusers="true" spamwhilebroken="true">
            <always address="tiet@informatik.uni-kiel.de"/>
            <failure address="tiet@informatik.uni-kiel.de"/>
        </email>
        -->

<!--
If you also want individual committers to receive email for all builds where they made changes, then set skipusers="false", and add a <map alias="cvsuser" address="cvsuser@mydomain.com"/>  element for each user inside the <email> element (replacing cvsuser with the real user id and email).
-->


        <htmlemail mailhost="mail.informatik.uni-kiel.de"
                returnaddress="tiet@informatik.uni-kiel.de"
                defaultsuffix="@informatik.uni-kiel.de"
                buildresultsurl="http://rtsys.informatik.uni-kiel.de:8180/cruisecontrol/buildresults/${project.name}"
                skipusers="false"
                spamwhilebroken="false"
                css="/home/local/cruisecontrol/cruisecontrol-2-7-1/reporting/jsp/webcontent/css/cruisecontrol.css"
                xsldir="/home/local/cruisecontrol/cruisecontrol-2-7-1/reporting/jsp/webcontent/xsl"
                logdir="logs/${project.name}">

                <map alias="tiet" address="tiet@informatik.uni-kiel.de" />
                <!--<map alias="oba" address="oba@informatik.uni-kiel.de" />-->
                <!--<map alias="haf" address="haf@informatik.uni-kiel.de" />-->

                <always address="tiet" />
                <!--<failure address="oba" reportWhenFixed="true" />-->
                <!--<failure address="haf" reportWhenFixed="true" />-->
        </htmlemail>

        <artifactspublisher
            dir="checkout/${project.name}/target"
            dest="artifacts/${project.name}"/>

    </publishers>
  </project>


  <!-- ############################################################################################################# -->
  <!-- ###########      NEW PROJECT: group4-org.eclipse.gmf.runtime.diagram.ui.properties          ################# -->
  <!-- ############################################################################################################# -->
  <project name="group4-org.eclipse.gmf.runtime.diagram.ui.properties" buildafterfailed="true">
    <listeners>
      <currentbuildstatuslistener file="logs/${project.name}/status.txt"/>
    </listeners>

    <!-- Bootstrappers are run every time the build runs, *before* the modification checks -->
    <bootstrappers>
        <!-- Handles updating a single file (typically build.xml and/or build.properties) from a Subversion repository before the build begins. -->
    </bootstrappers>

    <!-- Defines where cruise looks for changes, to decide whether to run the build (specified in seconds) -->
    <modificationset quietperiod="1800">
        <svn localworkingcopy="checkout/group4-amk-mmu/stable/org.eclipse.gmf.runtime.diagram.ui.properties"/>
        <!-- https://rtsys.informatik.uni-kiel.de/svn/teaching/prak/07ws-model/public/phase2/group4-amk-mmu/stable/org.eclipse.gmf.runtime.diagram.ui.properties -->
    </modificationset>

    <!-- Configures the actual build loop, how often and which build file/target (seconds to sleep in between polling for changes) -->
    <schedule interval="3600">
      <ant anthome="/usr/share/ant"
           buildfile="build-${project.name}.xml"
           target="metrics"
           uselogger="false"
           usedebug="false"/>
    </schedule>

    <!-- directory to write build logs to -->
    <log logdir="logs/${project.name}"/>

    <!-- Publishers are run *after* a build completes -->
    <publishers>

        <!--
        <email mailhost="mail.informatik.uni-kiel.de"
                returnaddress="tiet@informatik.uni-kiel.de"
                buildresultsurl="http://rtsys.informatik.uni-kiel.de/cc/buildresults/${project.name}"
                skipusers="true" spamwhilebroken="true">
            <always address="tiet@informatik.uni-kiel.de"/>
            <failure address="tiet@informatik.uni-kiel.de"/>
        </email>
        -->
<!--
If you also want individual committers to receive email for all builds where they made changes, then set skipusers="false", and add a <map alias="cvsuser" address="cvsuser@mydomai
n.com"/>  element for each user inside the <email> element (replacing cvsuser with the real user id and email).
-->
        <htmlemail mailhost="mail.informatik.uni-kiel.de"
                returnaddress="tiet@informatik.uni-kiel.de"
                defaultsuffix="@informatik.uni-kiel.de"
                buildresultsurl="http://rtsys.informatik.uni-kiel.de:8180/cruisecontrol/buildresults/${project.name}"
                skipusers="false"
                spamwhilebroken="false"
                css="/home/local/cruisecontrol/cruisecontrol-2-7-1/reporting/jsp/webcontent/css/cruisecontrol.css"
                xsldir="/home/local/cruisecontrol/cruisecontrol-2-7-1/reporting/jsp/webcontent/xsl"
                logdir="logs/${project.name}">

                <map alias="tiet" address="tiet@informatik.uni-kiel.de" />
                <!--<map alias="oba" address="oba@informatik.uni-kiel.de" />-->
                <!--<map alias="haf" address="haf@informatik.uni-kiel.de" />-->

                <always address="tiet" />
                <!--<failure address="oba" reportWhenFixed="true" />-->
                <!--<failure address="haf" reportWhenFixed="true" />-->
        </htmlemail>

        <artifactspublisher
            dir="checkout/group4-amk-mmu/stable/org.eclipse.gmf.runtime.diagram.ui.properties/target"
            dest="artifacts/${project.name}"/>

    </publishers>
  </project>




  <!-- ############################################################################################################# -->
  <!-- ###########         NEW PROJECT: group4-org.eclipse.gmf.runtime.diagram.ui                  ################# -->
  <!-- ############################################################################################################# -->
  <project name="group4-org.eclipse.gmf.runtime.diagram.ui" buildafterfailed="true">
    <listeners>
      <currentbuildstatuslistener file="logs/${project.name}/status.txt"/>
    </listeners>

    <!-- Bootstrappers are run every time the build runs, *before* the modification checks -->
    <bootstrappers>
        <!-- Handles updating a single file (typically build.xml and/or build.properties) from a Subversion repository before the build begins. -->
    </bootstrappers>

    <!-- Defines where cruise looks for changes, to decide whether to run the build (specified in seconds) -->
    <modificationset quietperiod="1800">
      <svn localworkingcopy="checkout/group4-amk-mmu/stable/org.eclipse.gmf.runtime.diagram.ui"/>
        <!-- https://rtsys.informatik.uni-kiel.de/svn/teaching/prak/07ws-model/public/phase2/group4-amk-mmu/stable/org.eclipse.gmf.runtime.diagram.ui -->
    </modificationset>

    <!-- Configures the actual build loop, how often and which build file/target (seconds to sleep in between polling for changes) -->
    <schedule interval="3600">
      <ant anthome="/usr/share/ant"
           buildfile="build-${project.name}.xml"
           target="metrics"
           uselogger="false"
           usedebug="false"/>
    </schedule>

    <!-- directory to write build logs to -->
    <log logdir="logs/${project.name}"/>

    <!-- Publishers are run *after* a build completes -->
    <publishers>

        <!--
        <email mailhost="mail.informatik.uni-kiel.de"
                returnaddress="tiet@informatik.uni-kiel.de"
                buildresultsurl="http://rtsys.informatik.uni-kiel.de/cc/buildresults/${project.name}"
                skipusers="true" spamwhilebroken="true">
            <always address="tiet@informatik.uni-kiel.de"/>
            <failure address="tiet@informatik.uni-kiel.de"/>
        </email>
        -->
<!--
If you also want individual committers to receive email for all builds where they made changes, then set skipusers="false", and add a <map alias="cvsuser" address="cvsuser@mydomai
n.com"/>  element for each user inside the <email> element (replacing cvsuser with the real user id and email).
-->
        <htmlemail mailhost="mail.informatik.uni-kiel.de"
                returnaddress="tiet@informatik.uni-kiel.de"
                defaultsuffix="@informatik.uni-kiel.de"
                buildresultsurl="http://rtsys.informatik.uni-kiel.de:8180/cruisecontrol/buildresults/${project.name}"
                skipusers="false"
                spamwhilebroken="false"
                css="/home/local/cruisecontrol/cruisecontrol-2-7-1/reporting/jsp/webcontent/css/cruisecontrol.css"
                xsldir="/home/local/cruisecontrol/cruisecontrol-2-7-1/reporting/jsp/webcontent/xsl"
                logdir="logs/${project.name}">

                <map alias="tiet" address="tiet@informatik.uni-kiel.de" />
                <!--<map alias="oba" address="oba@informatik.uni-kiel.de" />-->
                <!--<map alias="haf" address="haf@informatik.uni-kiel.de" />-->

                <always address="tiet" />
                <!--<failure address="oba" reportWhenFixed="true" />-->
                <!--<failure address="haf" reportWhenFixed="true" />-->
        </htmlemail>

        <artifactspublisher
            dir="checkout/group4-amk-mmu/stable/org.eclipse.gmf.runtime.diagram.ui/target"
            dest="artifacts/${project.name}"/>

    </publishers>
  </project>




  <!-- ############################################################################################################# -->
  <!-- ###########         NEW PROJECT: group1-org.eclipse.gmf.runtime.diagram.ui                  ################# -->
  <!-- ############################################################################################################# -->
  <project name="group1-org.eclipse.gmf.runtime.diagram.ui" buildafterfailed="true">
    <listeners>
      <currentbuildstatuslistener file="logs/${project.name}/status.txt"/>
    </listeners>

    <!-- Bootstrappers are run every time the build runs, *before* the modification checks -->
    <bootstrappers>
        <!-- Handles updating a single file (typically build.xml and/or build.properties) from a Subversion repository before the build begins. -->
    </bootstrappers>

    <!-- Defines where cruise looks for changes, to decide whether to run the build (specified in seconds) -->
    <modificationset quietperiod="1800">
      <svn localworkingcopy="checkout/group1-fsch-svk/devel/org.eclipse.gmf.runtime.diagram.ui"/>
        <!-- https://rtsys.informatik.uni-kiel.de/svn/teaching/prak/07ws-model/public/phase2/group1-fsch-svk/stable/org.eclipse.gmf.runtime.diagram.ui -->
    </modificationset>

    <!-- Configures the actual build loop, how often and which build file/target (seconds to sleep in between polling for changes) -->
    <schedule interval="3600">
      <ant anthome="/usr/share/ant"
           buildfile="build-${project.name}.xml"
           target="metrics"
           uselogger="false"
           usedebug="false"/>
    </schedule>

    <!-- directory to write build logs to -->
    <log logdir="logs/${project.name}"/>

    <!-- Publishers are run *after* a build completes -->
    <publishers>
        <!--
        <email mailhost="mail.informatik.uni-kiel.de"
                returnaddress="tiet@informatik.uni-kiel.de"
                buildresultsurl="http://rtsys.informatik.uni-kiel.de/cc/buildresults/${project.name}"
                skipusers="true" 
		spamwhilebroken="false">
            <always address="tiet@informatik.uni-kiel.de"/>
            <failure address="tiet@informatik.uni-kiel.de"/>
        </email>
        -->
<!--
If you also want individual committers to receive email for all builds where they made changes, then set skipusers="false", and add a <map alias="cvsuser" address="cvsuser@mydomai
n.com"/>  element for each user inside the <email> element (replacing cvsuser with the real user id and email).
-->
        <htmlemail mailhost="mail.informatik.uni-kiel.de"
                returnaddress="tiet@informatik.uni-kiel.de"
                defaultsuffix="@informatik.uni-kiel.de"
                buildresultsurl="http://rtsys.informatik.uni-kiel.de:8180/cruisecontrol/buildresults/${project.name}"
                skipusers="false"
                spamwhilebroken="false"
                css="/home/local/cruisecontrol/cruisecontrol-2-7-1/reporting/jsp/webcontent/css/cruisecontrol.css"
                xsldir="/home/local/cruisecontrol/cruisecontrol-2-7-1/reporting/jsp/webcontent/xsl"
                logdir="logs/${project.name}">

                <map alias="tiet" address="tiet@informatik.uni-kiel.de" />
                <!--<map alias="oba" address="oba@informatik.uni-kiel.de" />-->
                <!--<map alias="haf" address="haf@informatik.uni-kiel.de" />-->

                <always address="tiet" />
                <!--<failure address="oba" reportWhenFixed="true" />-->
                <!--<failure address="haf" reportWhenFixed="true" />-->
        </htmlemail>

        <artifactspublisher
            dir="checkout/group1-fsch-svk/devel/org.eclipse.gmf.runtime.diagram.ui/target"
            dest="artifacts/${project.name}"/>

    </publishers>
  </project>


  <!-- ############################################################################################################# -->
  <!-- ###########         NEW PROJECT: group1-org.eclipse.gef                                     ################# -->
  <!-- ############################################################################################################# -->
  <project name="group1-org.eclipse.gef" buildafterfailed="true">
    <listeners>
      <currentbuildstatuslistener file="logs/${project.name}/status.txt"/>
    </listeners>

    <!-- Bootstrappers are run every time the build runs, *before* the modification checks -->
    <bootstrappers>
        <!-- Handles updating a single file (typically build.xml and/or build.properties) from a Subversion repository before the build begins. -->
    </bootstrappers>

    <!-- Defines where cruise looks for changes, to decide whether to run the build (specified in seconds) -->
    <modificationset quietperiod="1800">
      <svn localworkingcopy="checkout/group1-fsch-svk/devel/org.eclipse.gef"/>
        <!-- https://rtsys.informatik.uni-kiel.de/svn/teaching/prak/07ws-model/public/phase2/group1-fsch-svk/stable/org.eclipse.gef -->
    </modificationset>

    <!-- Configures the actual build loop, how often and which build file/target (seconds to sleep in between polling for changes) -->
    <schedule interval="3600">
      <ant anthome="/usr/share/ant"
           buildfile="build-${project.name}.xml"
           target="metrics"
           uselogger="false"
           usedebug="false"/>
    </schedule>

    <!-- directory to write build logs to -->
    <log logdir="logs/${project.name}"/>

    <!-- Publishers are run *after* a build completes -->
    <publishers>
        <!--
        <email mailhost="mail.informatik.uni-kiel.de"
                returnaddress="tiet@informatik.uni-kiel.de"
                buildresultsurl="http://rtsys.informatik.uni-kiel.de/cc/buildresults/${project.name}"
                skipusers="true" spamwhilebroken="true">
            <always address="tiet@informatik.uni-kiel.de"/>
            <failure address="tiet@informatik.uni-kiel.de"/>
        </email>
        -->
<!--
If you also want individual committers to receive email for all builds where they made changes, then set skipusers="false", and add a <map alias="cvsuser" address="cvsuser@mydomai
n.com"/>  element for each user inside the <email> element (replacing cvsuser with the real user id and email).
-->
        <htmlemail mailhost="mail.informatik.uni-kiel.de"
                returnaddress="tiet@informatik.uni-kiel.de"
                defaultsuffix="@informatik.uni-kiel.de"
                buildresultsurl="http://rtsys.informatik.uni-kiel.de:8180/cruisecontrol/buildresults/${project.name}"
                skipusers="false"
                spamwhilebroken="false"
                css="/home/local/cruisecontrol/cruisecontrol-2-7-1/reporting/jsp/webcontent/css/cruisecontrol.css"
                xsldir="/home/local/cruisecontrol/cruisecontrol-2-7-1/reporting/jsp/webcontent/xsl"
                logdir="logs/${project.name}">

                <map alias="tiet" address="tiet@informatik.uni-kiel.de" />
                <!--<map alias="oba" address="oba@informatik.uni-kiel.de" />-->
                <!--<map alias="haf" address="haf@informatik.uni-kiel.de" />-->

                <always address="tiet" />
                <!--<failure address="oba" reportWhenFixed="true" />-->
                <!--<failure address="haf" reportWhenFixed="true" />-->
        </htmlemail>

        <artifactspublisher
            dir="checkout/group1-fsch-svk/devel/org.eclipse.gef/target"
            dest="artifacts/${project.name}"/>

    </publishers>
  </project>



</cruisecontrol>
